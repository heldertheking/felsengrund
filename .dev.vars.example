# Copy this file to .dev.vars for local development (gitignored, never commit real secrets).
# In production, set the real value with: wrangler secret put ADMIN_UPLOAD_PASSWORD
ADMIN_UPLOAD_PASSWORD=changeme
